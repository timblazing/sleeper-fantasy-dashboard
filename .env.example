# Optional. Unlocks POST /trade/calculate (the Trade Lab) and the key-gated
# /player-page endpoints behind player profiles. Everything else on RosterAudit
# works without it. Request one at https://rosteraudit.com.
ROSTERAUDIT_API_KEY=
